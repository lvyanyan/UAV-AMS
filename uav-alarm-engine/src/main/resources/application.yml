server:
  port: 8095 # 8082 被 wslrelay.exe 占用

spring:
  application:
    name: uav-alarm-engine
  kafka:
    bootstrap-servers: ${KAFKA_BOOTSTRAP:172.27.19.223:9092}
    consumer:
      group-id: alarm-engine
      auto-offset-reset: latest
      key-deserializer: org.apache.kafka.common.serialization.StringDeserializer
      value-deserializer: org.apache.kafka.common.serialization.StringDeserializer
    producer:
      key-serializer: org.apache.kafka.common.serialization.StringSerializer
      value-serializer: org.apache.kafka.common.serialization.StringSerializer
  redis:
    host: ${REDIS_HOST:172.27.19.223}
    port: ${REDIS_PORT:6379}
    database: 0
  datasource:
    url: jdbc:postgresql://${DB_HOST:172.27.19.223}:${DB_PORT:5432}/${DB_NAME:uav_ams}
    username: ${DB_USER:uav}
    password: ${DB_PASS:uav123}
    driver-class-name: org.postgresql.Driver

logging:
  level:
    com.uav.alarm: DEBUG
    org.apache.kafka: WARN
