server:
  port: 8081

spring:
  application:
    name: uav-system
  datasource:
    url: jdbc:postgresql://${DB_HOST:172.27.19.223}:${DB_PORT:5432}/${DB_NAME:uav_ams}
    username: ${DB_USER:uav}
    password: ${DB_PASS:uav123}
    driver-class-name: org.postgresql.Driver
  redis:
    host: ${REDIS_HOST:172.27.19.223}
    port: ${REDIS_PORT:6379}
    database: 0

mybatis-plus:
  mapper-locations: classpath*:/mapper/**/*.xml
  type-aliases-package: com.uav.system.entity
  configuration:
    map-underscore-to-camel-case: true

jwt:
  secret: ${JWT_SECRET:uav-ams-dev-secret-key-min-32-chars!!}
  expiration-ms: 7200000
  military-expiration-ms: 900000

logging:
  level:
    com.uav.system: DEBUG
